#include <iostream>

#define level 4

using namespace std;

bool Check(int* Q, int row, int col)
{
	for (int i = 0; i < level; i++)
	{
		if (Q[i] == Q[level])
			return false;
		if (Q[level]-Q[i] == 
		{

		}
	}
}



int main()
{
	// 퀸을 찾기 위한 메모리 할당
	int* Q = new int[level];

	for (int i = 0; i < level; i++)
	{
		Q[i] = 1;
		for (int j = 1; j < level; j++)
		{
			
			
		}

		
	}
	


}